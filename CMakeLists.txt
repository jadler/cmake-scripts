CMAKE_MINIMUM_REQUIRED (VERSION 3.9 FATAL_ERROR)

PROJECT ("Sample" VERSION 1.0.0 LANGUAGES CXX)

ADD_LIBRARY (${PROJECT_NAME})

SET_TARGET_PROPERTIES (${PROJECT_NAME}
	PROPERTIES
		LIBRARIES "Dependency library")

INCLUDE (CMakeConfig)
